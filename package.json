{
  "name": "section",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "web": "bun --cwd web dev",
    "build": "bun --cwd web build",
    "start": "bun --cwd web start",
    "lint": "bun --cwd web lint",
    "format": "bun --cwd web format",
    "app": "swift run",
    "appbuild": "swift build"
  }
}
